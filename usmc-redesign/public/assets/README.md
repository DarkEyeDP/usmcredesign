Historical CSS files in this directory are intentionally kept for analytics
session replay tools such as Microsoft Clarity.

GitHub Pages deploys replace the whole artifact, while older Clarity recordings
can still reference the hashed CSS filename that existed when the session was
captured. Keeping those files public prevents old recordings from replaying as
unstyled HTML.
